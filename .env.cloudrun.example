# Copy to .env.cloudrun and set your production URL.
# Merged with .env when running: ./scripts/push-env-to-cloud-run.sh
# Keeps .env with localhost for local dev; Cloud Run gets this URL so login redirects here.

# NEXTAUTH_URL=https://hireu-frontend-50507660058.us-central1.run.app
NEXTAUTH_URL=https://hire.hookstep.in

# Also add this redirect URI in your OAuth apps (Google, GitHub, etc.):
#   https://hire.hookstep.in/api/auth/callback/google
#   https://hire.hookstep.in/api/auth/callback/github
